{{- if .Values.runtimeConfig }}
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "platform-workload.fullname" . }}-runtime
  namespace: {{ include "platform-workload.namespace" . }}
  labels:
    {{- include "platform-workload.labels" . | nindent 4 }}
data:
  {{- $runtimeEnv := .Values.runtimeConfig.env | default .Values.runtimeConfig }}
  {{- range $key, $value := $runtimeEnv }}
  {{- if ne $key "files" }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
  {{- end }}
  {{- range $key, $value := .Values.runtimeConfig.files }}
  {{ $key }}: |
{{ tpl ($value | toString) $ | indent 4 }}
  {{- end }}
{{- end }}
{{- range $name, $config := .Values.configMaps }}
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ $config.name | default (printf "%s-%s" (include "platform-workload.fullname" $) $name) }}
  namespace: {{ include "platform-workload.namespace" $ }}
  labels:
    {{- include "platform-workload.labels" $ | nindent 4 }}
data:
  {{- range $key, $value := $config.data }}
  {{ $key }}: |
{{ tpl ($value | toString) $ | indent 4 }}
  {{- end }}
{{- end }}
